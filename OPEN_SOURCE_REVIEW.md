# 开源优先评估记录(Open-Source-First Review)

> **创建**:2026-08-01 · **依据**:项目要求"实施任何解决方案前,应在 GitHub 和 npm 上进行详尽研究"。
> 本文件记录本仓库所有(拟)引入依赖的开源优先评估:候选方案、评估标准、结论与自研豁免理由。
> 评估标准(项目规约):① 活跃维护(过去 6 个月内 ≥3 次更新)② 全面文档(安装/API/示例)③ 兼容许可证(MIT / Apache-2.0 等宽松许可)。

## 1. 评估方法

- **npm 研究**:`npm view <pkg> version license time`(版本/许可证/最近发布时间,实证);
- **GitHub 研究**:`search_repositories`(活跃度/文档/维护状态,实证);
- **复用优先**:满足三标准 → 复用;不满足或与核心模式深度耦合 → 自研(记录"确凿技术理由"豁免)。

## 2. 已复用方案(满足三标准)

| 方案                         | 版本/活跃证据                               | 许可证    | 用途                       | 评估结论                               |
| ---------------------------- | ------------------------------------------- | --------- | -------------------------- | -------------------------------------- |
| `@modelcontextprotocol/sdk`  | v1.30.0,2026-07-27 发布(6 个月内 ≥5 次更新) | MIT       | MCP 客户端(stdio 工具集成) | ✅ 复用;官方协议,零自定义              |
| `i18next`(+ `react-i18next`) | v26.3.6,2026-07-09 更新(活跃)               | MIT       | 0.3.0 M2 i18n(规划)        | ✅ 复用(规划阶段已评估,实现时锁定版本) |
| `pixelmatch` + `pngjs`       | 活跃维护,文档全                             | ISC / MIT | 视觉回归 diff(基线对比)    | ✅ 复用                                |
| `electron-log`               | 既有依赖,活跃                               | MIT       | 文本日志                   | ✅ 复用(不替换)                        |
| `@tanstack/react-virtual`    | 既有依赖,活跃                               | MIT       | 虚拟列表                   | ✅ 复用                                |
| `electron-window-state`      | 既有依赖,活跃                               | MIT       | 窗口状态持久化             | ✅ 复用                                |

## 3. 自研决策(确凿技术理由豁免,需项目相关方批准)

| 自研模块                             | 开源候选                                | 自研理由(豁免依据)                                                                                    |
| ------------------------------------ | --------------------------------------- | ----------------------------------------------------------------------------------------------------- |
| `structured-log.ts`(JSON Lines 日志) | 结构化日志库                            | 复用现有 electron-log 扩展 JSON Lines 成本极低;引入新依赖扩大 audit 面,无收益                         |
| `diagnostics.ts`(诊断导出)           | —                                       | 纯文件打包,无成熟通用方案;自研约 60 行                                                                |
| `skills.ts` + 技能加载(skills 市场)  | 通用 skills 协议(如 claude-code skills) | 通用方案无法复用本项目安全纵深(审批/工作区/patch/命名空间隔离);自研基于 shared 纯函数,与 MCP 模式对齐 |
| 视觉/启动/冷启动测试脚本             | —                                       | 测试资产,非生产依赖                                                                                   |

## 4. 研究证据(实证输出节选)

```
npm view @modelcontextprotocol/sdk → v1.30.0 / MIT / 2026-07-27
npm view i18next → v26.3.6 / MIT / 2026-07-09 更新
npm view pixelmatch,pngjs → 安装成功,API 验证通过
GitHub search "electron desktop AI agent" → cc-haha / CodePilot / LobsterAI 等,
  共性: MCP + 可观测性(本项目已对齐)
```

## 5. 结论

- 所有**新引入**的生产/开发依赖均满足三标准(活跃/文档/宽松许可),复用而非自研;
- 4 处自研均有"确凿技术理由"(安全纵深耦合 / 避免依赖膨胀 / 无成熟通用方案),符合豁免条款;
- 引入新依赖前(如 0.3.0 i18next)需复跑 `npm audit`,确认不新增高危链。
