# 2026-07-27

- 延续 HANDOFF 性能工作，未重复规范化与主题改造。
- 将 MarkdownContent（react-markdown + remark-gfm + rehype-highlight + rehype-sanitize 及代码块渲染）整体设为懒加载边界；生产构建主 renderer chunk 从 1,203.59KB 降至 731.46KB（-472.13KB / -39.2%），按需 Markdown chunk 738.05KB。
- 验证：lint、双 tsconfig typecheck、production build 通过。Vitest 在加载 tests/unit.test.ts 时发生运行时内部错误 `Cannot read properties of undefined (reading 'config')`，0 tests，复跑稳定复现，与本轮 renderer 拆分无直接导入关系，完整 verify 因此未全绿。
- 2000 条虚拟滚动注入与 FPS 监控入口已存在；正式 FPS 数值仍须在 Electron 图形窗口中执行实际滚动或接入 E2E 自动化，当前未虚构实测结果。
- 性能改动审计发现：生产主包仍包含 dev 压测生成器/FPS 工具；FPS 平均算法使用逐帧 FPS 算术平均而失真；压测替换当前内存消息且停止后不恢复；Markdown 懒加载会在存在 assistant 消息时立即加载 738KB chunk；无语言 fenced code 的 CodeBlockPre 可能提取为空。当前改动不宜直接合并，应先修复 P1/P2。
- 已完成审计整改：dev 压测生成器/FPS 模块改为带 DEV 守卫的动态导入，生产主 chunk 已确认不含测试实现；压测停止/卸载恢复原消息并处理双击、异步卸载及会话切换竞态；FPS 改用 frame time、总时长平均 FPS、nearest-rank P50/P95/P99 和慢帧分档；无语言 fenced code 正常回退 text。
- Vitest 4.1.10 在当前环境连最小 describe 冒烟测试也因 runner.config undefined 崩溃，降级至与 Vite 6 兼容的 3.2.4 后恢复。最终 `npm run verify` 全绿（lint、双 tsconfig、123 tests、build）；主 renderer 726.51KB，Markdown 按需 chunk 738.02KB。
- 端到端加固继续完成：Vitest 升至安全兼容版 3.2.6；FPS 统计抽到 `src/shared/fps-stats.ts` 并加入数学边界测试；Electron 主窗口增加同源导航与 popup 拦截，所有 preload IPC handler 统一 sender 校验及输入边界。最终 `npm run verify` 全绿（129 tests），生产依赖 audit 0；全依赖遗留 19 high + 1 moderate 均在开发构建链，拒绝 npm 建议的 electron-builder 强制降级。
- Windows 打包首次因已有 `dist/win-unpacked` 触发批量删除保护而失败；未绕过保护，改用 `dist-hardening/` 新目录后 NSIS setup、portable、win-unpacked 与 latest.yml 均成功生成。真实 2000 条 Electron FPS、GUI UAT、签名发布仍明确保留为外部/真机验收项，未虚构通过。
- 同步更新 `PERFORMANCE_REPORT.md`、`OPTIMIZATION_ROADMAP.md`、`RESIDUAL_RISKS.md`、`CHANGELOG.md`、`tests/SELF_CHECK.md`、`OPEN_SOURCE_SEARCH.md`，以当前 bundle、129 tests、安全边界和残余风险为准。
- 完成 Dave Desktop 七维全面工程审查并生成 `outputs/Dave_Desktop_全面工程审查报告_2026-07-27.md`；确认 verify 全绿、生产依赖 0 漏洞，但全依赖有 19 high + 1 moderate 开发链告警、Prettier 有 10 文件不一致、coverage provider 缺失。最高优先风险为 shell 的 cwd 不构成沙箱、custom Provider SSRF/HTTP、safeStorage 明文降级、中止 partial 持久化竞态和 patch 非事务性；本轮未修改业务源码与既有文档。
- 对全部审查项执行落地复验并生成 `outputs/Dave_Desktop_审查整改落地验收_2026-07-27.md`。验收不通过：P0 0/2、P1 0/11 完整关闭；上一轮启动的五项关键整改未出现在实际工作区。当前 verify 仍全绿（129 tests/build），生产 audit 0，但 format check 有 11 文件不一致、全依赖仍 19 high + 1 moderate，覆盖率/Electron E2E/真机性能未闭环。文案仅部分修正，不能替代安全实现。
- 归档前复核：当前只能做“阶段性快照归档”，不能标记为“最终稳定版/可发布归档”。工作树仍有大量未提交改动与未跟踪文件，且落地验收明确为 P0 0/2、P1 0/11；应先形成可追溯 commit/tag，再按风险顺序整改和复验。
- 主会话接管并完成一批高优先整改：所有 Shell-capable 工具强制审批；Custom Provider 启用 HTTPS/公网 DNS/每跳重定向校验并在跨源跳转剥离鉴权；API Key 使用 `safe-storage:v1:` envelope 且拒绝明文降级；abort partial 改由主进程持久化，renderer 以主进程为单一事实源并处理 invoke reject；多文件 Patch 改为全量预计算和失败逆序回滚。新增 Patch 回归后单测 138/138；`npm run verify` 已包含格式、覆盖率、lint、双类型检查、测试和构建并全绿，生产依赖审计 0。复验报告已更新；OS Shell 沙箱、DNS rebinding 连接固定、Electron GUI/E2E、低覆盖率、bundle 体积和其余 P2/P3 仍未关闭。
- 继续完成 Electron 与可靠性加固：renderer 启用 sandbox；IPC 绑定唯一主窗口、顶层 frame 和可信 URL；生产 CSP 移除脚本 unsafe-inline 与 localhost/ws（dev 运行时单独注入）；SSE 支持 CRLF、多行 data、尾部 flush 与限长；会话 JSON 损坏时备份并记录错误。最终 `npm run verify` 全绿（138 tests，coverage statements 32.74%/branches 71.84%/functions 56.30%）；生产依赖 audit 0。Playwright Electron smoke 已接入但进程握手失败/挂起，Windows 最终打包耗时异常，均未虚报通过。
