# Dave Desktop 六维分析报告(截至 2026-07-31)

> 分析对象:Dave Desktop 客户端(v0.1.0 → 0.2.0 目标)
> 数据采集时间:2026-07-31(本会话实测 typecheck / 150 tests / bundle 产物)
> 数据来源:`git log`、`tests/unit.test.ts` 实测、`PERFORMANCE_REPORT.md`、`HANDOFF.md`、`RESIDUAL_RISKS.md`、`src/shared/telemetry.ts`

---

## 一、按最新值增强(目标动态校准)

**数据来源与时效性**:全部数据来自仓库内实测/文档,采集时间 2026-07-31 13:51,构建/测试数据每次改动后即可重跑,遥测漏斗事件实时落本地 electron-store(环形缓冲 5000 条,更新频率 = 事件发生频率)。

| 指标                | 最新值(截至 2026-07-31)                                 | 采集方式                                 | 目标(0.2.0)           | 状态                       | 与目标的关联性                          |
| ------------------- | ------------------------------------------------------- | ---------------------------------------- | --------------------- | -------------------------- | --------------------------------------- |
| 单元测试            | **150/150 通过**                                        | `npm test` 实测                          | >150                  | ✅ 达标                    | 质量门禁硬指标                          |
| typecheck           | 双 tsconfig 零错误                                      | `npm run typecheck` 实测                 | 零错误                | ✅ 达标                    | 门禁硬指标                              |
| 主 renderer bundle  | **≈745.72KB**                                           | `out/bundle-stats.html`(2026-07-29 构建) | <900KB                | ✅ 达标(-39.6% 于 1,203KB) | 冷启动/首屏感知                         |
| Markdown 按需 chunk | **≈738KB**                                              | 同上                                     | 建议 <500KB           | ⚠️ 偏大                    | 首条消息 TTFB 渲染感知                  |
| 冷启动到窗口        | **~3s**(估算)                                           | 产品规约估算,未打点验证                  | <1.5s                 | ❌ 未达                    | 性能预算 COLD_WINDOW_BUDGET=3s 恰在边界 |
| 1000 消息滚动帧率   | **未采集**(工具就绪)                                    | FPS-REAL 风险项,需真机 Gauge 按钮        | >50fps                | ⏳ 待验证                  | 虚拟列表收益的唯一实证                  |
| 生产依赖安全        | **audit --omit=dev = 0 漏洞**                           | `npm audit`                              | 0                     | ✅ 达标                    | 发布安全门禁                            |
| 全量依赖审计        | 19 high + 1 moderate(dev 链)                            | `npm audit`                              | 等待上游              | ⚠️ 外置                    | electron-builder/eslint 传递链          |
| 生产安装包          | `dist/dave-desktop-win-x64-setup.exe` + portable ~108MB | 2026-07-27 打包                          | 可分发                | ⚠️ 未签名                  | 分发转化前提                            |
| 转化漏斗数据        | **尚无真实数据**(埋点 27 事件 + computeFunnel 就绪)     | 本地遥测 store                           | firstMessageRate 基线 | ⏳ 待发布后采集            | "打磨提升转化率"的核心目标无数据驱动    |

**动态增强结论**:目标定义无需修正,但需按最新值做三处细化——① bundle 目标已达成,绩效重心从"瘦身"转移到"Markdown chunk 738KB 的首次加载感知";② 冷启动 3s 仅触及预算边界(3s 内视为达标),与 0.2.0 的 <1.5s 目标有 2 倍差距,须列为可验证优化项;③ 代码级可收口项已全部关闭(风险台账 15/15),剩余瓶颈全部是"真实环境证据"(FPS 真机、远端 CI、签名),意味着下一步投入应从写代码转向采证据与搭发布链路。

---

## 二、优化点识别(按影响优先级)

| 优先级   | 优化点                                   | ①问题描述与影响范围                                                                                                 | ②根因分析                                                                   | ③预期收益(量化)                                                                       | ④优先级判定依据                                                                                   |
| -------- | ---------------------------------------- | ------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------- | ------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------- |
| **P0-1** | FPS 真机采集                             | 2000 条混合消息滚动性能无实证;虚拟列表/120ms 节流/memo 三项优化收益无法验收;是否需进一步优化无法决策                | 需人工在目标 Windows 机操作(`npm run dev` → Gauge → 滚动),此前无机器/无时间 | 消除 P1 风险;解锁后续性能投入方向决策;**成本≈2 分钟,收益=验收证据**                   | 零外部依赖、成本近零、阻塞后续 3 个决策点,ROI 最高                                                |
| **P0-2** | E2E 全链路自动化                         | smoke 仅覆盖 CSP/帮助/面板/新建会话/导出/Ctrl+F;真实流式、编辑再生成、Agent 批准、patch 应用无自动化断言,回归风险高 | 需真实 API Key;GUI 断言需 Playwright 脚本,之前未投入                        | 发布可信度提升;CI 可拦截 UI 回归;**场景数从 7 → 10+**                                 | 无外部阻塞(仅需 Key),直接服务发布门禁                                                             |
| **P1-1** | 冷启动优化                               | 冷启动 ~3s,0.2.0 目标 <1.5s;首屏感知是用户第一印象,直接影响留存                                                     | 主进程同步初始化 electron-log/updater/store;窗口等全部就绪才显示            | 冷启动 **~3s → <1.5s(2 倍提速)**;符合 COLD_WINDOW_BUDGET=3s 预算的余量翻倍            | 用户可感知、无外部依赖,roadmap 已列方案(lazy require + 窗口先显)                                  |
| **P1-2** | Markdown chunk 瘦身                      | 738KB 按需 chunk 在首条消息时加载,主导 TTFB 渲染感知;highlight.js 全语言库进包                                      | rehype-highlight 默认引入全语言高亮 + 完整 remark/rehype 链                 | chunk **738KB → <500KB(降 ~30%+)**,首条消息渲染提速                                   | 主包已饱和(745KB 主要为 React),剩余可优化空间集中在 Markdown 链;需先完成 P0-1 取得 FPS 基线再动手 |
| **P1-3** | 签名 + 自动发布                          | 无代码签名证书,SmartScreen 拦截、无法配置 electron-updater 发布                                                     | 证书需采购($200/年);发布策略未建立                                          | 安装包通过 SmartScreen;**分发转化率提升 + 遥测漏斗才有真实数据源**                    | 外部资金依赖,但它是 P1-4 遥测闭环的前置                                                           |
| **P1-4** | 遥测数据闭环                             | 27 个事件埋点 + 漏斗计算已就绪,但无真实数据、无看板,onboarding 三屏设计是假设驱动                                   | 应用未正式发布,无用户;漏斗数据需 7 日回访窗口积累                           | 拿到 firstMessageRate 基线 → 数据驱动 onboarding 向导迭代(转化率核心目标从"猜"变"测") | 依赖 P1-3 发布,属发布后红利                                                                       |
| **P2-1** | 主进程 Worker 化                         | agent 工具循环(shell/写文件)阻塞主线程,复杂任务期间 UI 卡顿                                                         | toolShell/toolWriteFile 直接在主进程执行                                    | 主线程空闲率 **>80%**;大 patch 内存峰值 **<200MB**                                    | 长任务场景才触发,当前影响面小,放发布稳定后                                                        |
| **P2-2** | 视觉回归测试                             | 主题/组件样式无截图对比基线,改动样式易静默回归                                                                      | Playwright 截图对比未接入 CI                                                | 主题(light/night)与 39 色彩变量回归 0 漏检                                            | 依赖 P0-2 的 E2E 基建,串行追加                                                                    |
| **P3**   | SQLite 迁移 / macOS/Linux / 开发链 audit | electron-store 规模够用;仅 Windows 环境;19 high 在 dev 链                                                           | 数据量未触发;无跨平台机器;上游依赖未修复                                    | —                                                                                     | 全部为"触发条件未到",保持监控不投入                                                               |

---

## 三、核心洞察提取

**洞察 1 — 代码质量已到"收口边界",剩余瓶颈全是环境证据而非代码缺陷**

- 支撑依据:风险台账 15 项代码级风险全部关闭;剩余 9 项中 P1 四项(FPS-REAL / UAT-E2E / SIGNING / UPDATE-RELEASE)无一可纯靠写代码关闭。
- 决策价值:意味着下一步的资源分配应从"写代码"转向"采集证据(真机 FPS、远端 CI 绿灯)与搭发布链路(证书、Releases)",代码级优化的边际收益已趋零,再投入属于浪费。

**洞察 2 — 主包优化已饱和(-39.6%),感知瓶颈转移到 738KB Markdown chunk**

- 支撑依据:主 bundle 1,203.59KB → 745.72KB;但 Markdown 渲染链(react-markdown + remark/rehype + highlight)独立 chunk ≈738KB,首条 assistant 消息时才加载。
- 决策价值:意味着后续性能工作应聚焦"首问渲染路径"——highlight 语言子集化、更轻高亮器,而非继续挤主包;也解释了为何"首屏 2s"已达标但"首问 TTFB"仍有感知提升空间。

**洞察 3 — 纯函数共享层是本代码库的最高 ROI 模式**

- 支撑依据:43 个 describe、150 条单测在 node 环境零 electron mock 跑通,覆盖 shell-policy / store-policy / rate-limit / message-search / computeFunnel / checkStartupBudget 等;AGENTS.md 反复强调"校验逻辑放 shared 纯函数,main 只做 adapter"。
- 决策价值:意味着新功能(文件下载校验、日志级别、搜索优化)一律先落 shared 纯函数,即可免费获得单测与三端复用;这是本仓库被验证过的质量杠杆。

**洞察 4 — 转化率目标当前是"假设驱动",发布 + 7 日窗口后才转"数据驱动"**

- 支撑依据:onboarding 三屏 + ApiKeyWizard 四步被 AGENTS.md 标为"转化率核心";27 个漏斗事件 + computeFunnel 就绪;但应用未正式分发,launched → first_message_sent 的真实比值不存在。
- 决策价值:意味着 onboarding 优化的正确顺序是"先发布拿基线,再迭代",任何发布前的向导改动都缺对照数据;应把遥测看板列为发布后的第一个里程碑。

**洞察 5 — 安全纵深是"全链路守卫 + 守卫可测"范式,应作为新边界的模板**

- 支撑依据:同一层叠模式贯穿所有信任边界——IPC 白名单(isAllowedStoreKey)+ sender 校验(validateSender)+ 限流(createRateLimiter)+ Markdown sanitize(schema 白名单)+ shell hard-deny + 导航同源;每个守卫都是纯函数且有单测。
- 决策价值:意味着 roadmap 中未落地的"文件下载校验 / 请求签名"应沿用同一模板(shared 纯函数 + main adapter + 单测),不要另起炉灶。

**洞察 6 — ESM/CJS + 双 tsconfig 的工程成本已被"规约化",风险不在新功能而在回归**

- 支撑依据:AGENTS.md 记录大量 gotcha(stripCrossorigin、CJS-shim 重入 guard、lifecycle 循环依赖、rehype-sanitize 类型);HANDOFF 踩坑表 11 条。
- 决策价值:意味着所有新 IPC handler / tray / 快捷键必须遵守既有 guard 模式(registered 标志、IME 保护、mounted 守卫),评审时优先检查"有没有绕过既有模式"。

---

## 四、拓扑分形分析(多层级拆解)

**宏观层(产品形态 · 会话流转拓扑)**

- 拓扑:单入口漏斗 `app_launch → onboarding_completed → workspace_chosen → first_message_sent`,7 日回访构成留存环;主路径 = 用户从安装到首次产出价值。
- 分形特征:整个产品是"入口 → 校验 → 落库 → 反馈"的重复:引导(三屏+向导)→ 会话流(发消息→流式→落库)→ 工具循环(agent 调用→审批→patch 应用→结果反馈)。

**中观层(模块边界 · 三进程拓扑)**

- 拓扑:`shared/`(纯函数,三端共享)→ `main/`(adapter + 状态)→ `preload/`(contextBridge 窄口)→ `renderer/`(React 消费);每条 IPC 边都是 `校验(shared) → 限流(main) → 执行 → 返回`。
- 自相似:与宏观层同构——每个边界都有一道"守卫 + 守卫可单测"。shared 层是结构枢纽:shell-policy/store-policy/rate-limit/message-search 全在此,被 main 与 renderer 双向引用。

**微观层(执行热路径 · 渲染时序)**

- 拓扑:`onChunk → throttle(120ms) → streamingContent → MessageBubble(memo) → 虚拟列表(item 复用)`;长会话中只有末条消息的 props 变化。
- 自相似:每级都是"输入 → 节流 → 增量更新",与宏观漏斗的"事件 → 去重 → 计数"同构。

**跨层级关联与传导路径**

- 关键传导链:`主进程初始化(3s)` → `窗口显示` → `首问 TTFB` → `Markdown chunk 加载(738KB)` → `流式渲染(120ms 节流)` → `长会话滚动(虚拟列表)`。此链上任一环节失守都表现为用户可感知的延迟,而各环节的修复手段(预算打点、懒加载、节流、memo)恰好构成全链覆盖。
- 共性规律:三个层级呈现同一自相似结构——**"入口守卫 → 节流 → 增量/去重 → 证据化"**;宏观(漏斗去重)、中观(IPC 白名单/限流)、微观(120ms 节流/memo)全是这一模式的实例。
- 层级特异性:宏观层的特异性指标是转化率与留存;中观层是边界正确性(校验拒绝率、限流触发数);微观层是帧耗时(P50/P95/P99 与慢帧率)。三个尺度共用的验证铁律是:**收益必须在真机/真实数据上实证**(FPS-REAL、漏斗数据、TTFB 打点)。

---

## 五、树结构优化(信息分层,深度 ≤4)

```
Dave Desktop 0.2.0 目标(根节点)
├─ 1. 性能(叶子·P0)
│  ├─ 1.1 冷启动 <1.5s(当前 ~3s,❌)
│  ├─ 1.2 首屏 bundle 745KB < 900KB(✅)
│  ├─ 1.3 滚动 >50fps @1000 消息(⏳ 待真机)
│  └─ 1.4 Markdown chunk <500KB(当前 738KB,⚠️)
├─ 2. 质量门禁(叶子·P0)
│  ├─ 2.1 单测 150/150(✅)
│  ├─ 2.2 E2E smoke 基线(✅)→ 全链路(⏳)
│  └─ 2.3 CI 远端首绿(⏳)
├─ 3. 分发链路(叶子·P1)
│  ├─ 3.1 代码签名(⏳ 待证书)
│  ├─ 3.2 auto-update 发布策略(⏳)
│  └─ 3.3 跨平台 smoke(⏳ P3)
├─ 4. 数据闭环(叶子·P1)
│  ├─ 4.1 遥测埋点 27 事件(✅)
│  └─ 4.2 漏斗基线 + 看板(⏳ 发布后)
└─ 5. 安全纵深(叶子·✅ 维护态)
   ├─ 5.1 IPC 白名单 + sender + 限流(✅)
   ├─ 5.2 shell 策略 + sanitize + 导航(✅)
   └─ 5.3 新边界模板(文件下载校验等,⏳)
```

**结构说明**:根节点=0.2.0 目标;5 个父节点=五大工作域;13 个叶子=可独立验收项。每个叶子都带状态标记(✅ 已闭环 / ⏳ 待办 / ⚠️ 偏大),导航效率 = 一眼定位"哪些叶子能立刻动、哪些被外部阻塞"。

---

## 六、下一步建议(含依赖与协同)

| 建议                             | ①明确目标                                           | ②关键实施步骤(≤5)                                                                                           | ③量化评估标准                                                          | ④完成时间节点                    |
| -------------------------------- | --------------------------------------------------- | ----------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------- | -------------------------------- |
| **R1:FPS 真机采集**              | 拿到 2000 消息滚动性能实证,作为 1.3 验收            | 1. `npm run dev` 2. 点 Gauge 注入 2000 条 3. 手动滚动 4. 记录报告写入 PERFORMANCE_REPORT.md                 | avg **>50fps**;P95 **<30ms**;P99 **<50ms**                             | **今日内**(约 2 分钟,无外部依赖) |
| **R2:E2E 全链路**                | smoke 从 7 场景扩到 10+,覆盖流式/编辑/批准          | 1. 准备真实 API Key 2. 补发消息→流式断言 3. 补编辑→截断再生成断言 4. 补 Agent 批准断言 5. CI 稳定绿         | 场景 **≥10**;CI 连续 **3 次**绿                                        | 1-2 天                           |
| **R3:冷启动优化**                | 冷启动 ~3s → <1.5s                                  | 1. electron-updater/log 改 lazy require 2. 窗口先显示再初始化 store 3. 打点验证 COLD_WINDOW_BUDGET          | cold window **<1.5s**;预算打点 within                                  | 本周(依赖 R1 拿到基线)           |
| **R4:Markdown chunk 瘦身**       | 738KB → <500KB                                      | 1. 按需注册 highlight 语言子集(ts/js/json/bash…) 2. 评估轻量高亮器 3. bundle 复测                           | chunk **<500KB**;首问渲染感知无劣化                                    | 本周(依赖 R1 基线防回归)         |
| **R5:签名 + 自动发布**(R3/R4 后) | 安装包可分发、updater 可用                          | 1. 采购证书($200/年) 2. 配置 build.win.certificateFile 3. 建 GitHub Releases + latest.yml 4. staged rollout | SmartScreen 通过;`latest.yml` 生效                                     | 2-3 周(外部采购路径)             |
| **R6:遥测漏斗复盘**(R5 发布后)   | 拿到 firstMessageRate 基线,数据驱动 onboarding 迭代 | 1. 发布后等 7 日回访窗口 2. computeFunnel 出基线 3. 对比 onboarding 各步事件流失 4. 迭代向导                | 拿到 **onboardRate / firstMessageRate / retentionRate** 基线并建立环比 | 发布后 7-14 天                   |

**依赖与协同关系**:R1 → R3、R4(真机基线决定性能投入方向与防回归锚点);R2 与 R1 并行;R3/R4 并行但都锚定 R1;R5 前置条件是 R1/R2 全绿(门禁可信);R6 严格依赖 R5 的正式分发。协同效应:R3 的预算打点与 R6 的遥测共用 `telemetry-store` 通道,R2 的 Playwright 基建可零成本扩展出视觉回归(P2-2),R4 的 chunk 瘦身与 R1 的 FPS 报告共同回答"0.2.0 性能是否达标"这一个验收问题。

---

**一句话总结**:代码级优化已收口(150 测试、bundle -39.6%、安全纵深闭环),0.2.0 剩余工作 90% 是"证据采集与发布链路"而非新功能——先做 R1(2 分钟),后续所有决策就有锚点。
